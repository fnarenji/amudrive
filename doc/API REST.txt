Tous les retours sont en JSON.

/auth
	/auth/login/{username}/{password_sha512}
		Authentification de l'utilisateur. Le token retourné est stocké en tant que cookie sur le client, et est vérifié à chaque requête suivante.
			token d'authentification au format suivant, cryptographié par clé symétrique (AES256), en base 64:
			{user_id}:{timestamp}:{ip}
			
		{username}
			le nom d'utilisateur
			
		{password_sha512}
			le mot de passe haché par SHA512

		{returns}
			true/false

	/auth/logout
		Détruit le token d'authentification courant

/profile
	/profile
		{returns}
			le profile complet de l'utilisateur

	[A faire pour chacune des propriétés de la table "client"]
	/profile/firstName/{firstName}
		{firstName}
			le nouveau prénom
			
		{returns}
			true/false

/vehicles
	/vehicles
		{returns}
			Tous les véhicules et leurs caractéristiques

	[A faire pour chacune des propriétés de la table "vehicle"]
	/vehicles/{idv}/smoking/{smoking}
		{idv}
			L'id du véhicule
		
		{smoking}
			Fumeur (booléen)
		
		{returns}
			true/false

/campus
	/campus
		{returns}
			Tous les campus

/carpooling
	/carpooling/search/{campus}/{address}/{areasize}/{direction}
		{campus}
			Id du campus
		
		{address}
			...
			
		{areasize}
			Taille de zone de recherche autour de l'addresse
		
		{direction}
			true: campus -> adresse
			false: adresse -> campus
		
		{returns}
			gogole
	
	/carpooling/{id}/join
		{id}
			Id du carpooling
	
	/carpooling/{id}/cancel
		{id}
			Id du carpoolnig
	
	/carpooling/{id}/comments
		{id}
			Id du carpooling
		
		{returns}
			Les commentaires associé au profil qui les a posté
		
	/carpooling/{id}/comments/{comment}/{marks}
		{id}
			Id du carpooling
			
		{comment}
			Contenu du commentaire
			
		{marks}
			Notes sur 5
		
		{returns}
			true/false
	
	/carpooling/{id}/comments/reply/{idcomment}/{reply}
		{id}
			Id du carpoolnigga
		
		{idcomment}
			Id du commentaire
		
		{reply}
			Contenu de la reponsé
			